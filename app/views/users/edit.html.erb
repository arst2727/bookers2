<h1>プロフィール画像編集画面</h1>
<!-- form_with部分 -->
<%= form_with model: @user,url:"/users/#{@user.id}", local: true do |f| %>
  <%# ここからエラーメッセージ %>
  <% if @user.errors.any? %>
    <%= @user.errors.count %> errors prohibited this book from being saved:<br>
      <% @user.errors.full_messages.each do |message| %>
        ・<%= message %><br>
      <% end %>
  <% end %>
  <%# ここまでエラーメッセージ %>
  <h4>name</h4>
  <%= f.text_field :name %>

  <h4>introduction</h4>
  <%= f.text_area :introduction %>

  <div><%= f.submit 'Update User Info' %></div>

<% end %>